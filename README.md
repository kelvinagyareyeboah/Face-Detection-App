# Face-Detection-App
A real-time face detection application built in C++ using OpenCV's Haar Cascade classifier. The app captures live video from a webcam, detects faces in each frame, draws bounding boxes around them, and displays a face count on screen.

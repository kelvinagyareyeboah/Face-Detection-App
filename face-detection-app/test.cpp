#include<o

	CascadeClassifier 
		video.r
	
			rectangle(im[i].tl(), faces[i].brar(
			rectangle(img, Point(0,0), Poi,70), Scalar(50, 50, 255), FILLED);
			putText(img, to_string(faces.size())+" Face Found", Point(10, 40), FONT_HERSHEY_DUPLEX, 1,

		
		imshow("Frame", img);
		waitKey(1);
		


#include<o

	CascadeClassifier 

	
			rectangle(im[i].tl(), fac
			rectangle(img, Point(0,0), Poi,70), Scalar(), FILLED);
			putText(img, to_string(faces.size())

		
		imshow("Frame", img);
		waitKey(1);
		


#include<o

	CascadeClassifier 

	
			rectangle(im[i].tl(), fac
			rectangle(img, Point(0,0), Poi,70), Scalar(50, 50, 255), FILLED);
			putText(img, to_string(faces.size())+" Face Found", Point( FONT_HERSHEY_DUPLEX, 1,


		
		imshow("Frame", img);
		waitKey(1);
		

